[project]
name = "portfolio"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "fastapi==0.115.6",
    "uvicorn[standard]==0.35.0",
    "pydantic>=2.0",
    "python-dotenv>=1.0.0",
    "langchain-core>=0.3.0",
    "deepagents>=0.6.0",
]

[tool.vercel]
entrypoint = "python.index:app"
